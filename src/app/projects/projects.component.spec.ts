import { ProjectsComponent } from './projects.component';

describe('Projects', () => {
  test('Test projects component', () => {
    const comp = new ProjectsComponent();
    expect(comp).toBeDefined();
  });
});

import { ToggleSidenavDirective } from './toggle-sidenav.directive';

describe('ToggleSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
